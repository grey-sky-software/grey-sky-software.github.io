{
  "name": "grey-sky-software-website",
  "version": "1.0.0",
  "private": true,
  "description": "The code that makes up the official Grey Sky Software website ",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/grey-sky-software/website.git"
  },
  "engines": {
    "node": "16.15.0",
    "npm": "8.5.5"
  },
  "scripts": {
    "build": "rm -rf dist && parcel build src/index.html --no-cache --dist-dir docs",
    "dev": "rm -rf dist && parcel src/index.html --no-cache --dist-dir docs",
    "lint": "eslint --ext .js src/",
    "start": "npm run dev"
  },
  "author": "Grey Sky Software",
  "license": "AGPL-3.0",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0",
    "parcel": "^2.5.0"
  }
}
